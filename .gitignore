# Per-user state — not in version control
/branches/
/data

# Go
*.exe
*.test
*.out
/orchestrator/bin/
/orchestrator/homa
/sandbox/probe/probe

# Node / Vite
**/node_modules/
/editor/dist/
**/.svelte-kit/
**/build/
*.log

# Note: orchestrator/internal/static/dist/ IS committed — go:embed needs it
# at compile time. Rebuild via ~/homa/editor/build.sh.

# Editor / IDE
.vscode/
.idea/
*.swp
.DS_Store

# SQLite scratch
*.db
*.db-journal
*.db-wal
*.db-shm

# Local config + built binaries at repo root
/config.json
/homa
config.local.json
.env
.env.local
/backups/
